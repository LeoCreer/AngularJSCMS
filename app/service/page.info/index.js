cms.factory('pageInfo', function() {
	var pageInfo = {
		// callbackList :[],
		// watchTitle: function(callback){
		// 	//Store function into Array
		// 	pageInfo.callbackList.push(callback);
		// },
		// setTitle: function(title){
		// 	// Loop through Array of functions
		// 	for (index in pageInfo.callbackList){
		// 		// Short Cut
		// 		//pageInfo.callbackList[index](title);

		// 		// Store function in var
		// 		var callback = pageInfo.callbackList[index];
		// 		// Call function and pass in title
		// 		callback(title);
		// 	}
		// }
		
	}
	return pageInfo;
  });


